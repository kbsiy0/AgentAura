# Tasks · `change/m4-icon-form`

Spec：`docs/superpowers/specs/2026-09-09-m4-icon-form-design.md`（§ 編號引用它）。
每 task 一 commit、TDD、pre-flight `git branch --show-current` = `change/m4-icon-form`。
Batch 內序列執行；batch 之間過 spec-reviewer phase-2。

| Task | 內容 | 綠燈條件 | 依賴 |
|---|---|---|---|
| **T01 測試底座** | 新增 `IconPaletteTests`（`paletteSubscriptIsTotal`、`appearanceCarriesPaletteColor`）/ `AnimationCurveTests` / `AppLayerSourceScanTests`（`appLayerNeverSwitchesOnIconAnimation`，掃描函式參數化；正向對照 probe 放**暫存目錄**，絕不寫 `Sources/`）；`OffscreenRender` sRGB harness；`RendererPixelTests`（`renderersDrawEveryActivityWithoutCrash`、`renderersHonorPalette`×2 五 activity、`plateGuaranteesContrast`、`workingIsQuietestColor`、`haloRingAnimatesCoreDoesNot`；harness 取樣越界 throw、`peakPhase` 由曲線掃）；`EvidenceRenderer`（env gate，先只有骨架）；`CompositionSmokeTests` 加 `iconFormSelectionIsWired`（三案例同一 `@Test`）、`statusItemWidthFollowsRenderer`。**只加 compile-only stub**（型別存在、回明顯錯的值，**不得** `fatalError`）；等式型 gate 一律用非預設 palette（spec §6 stub 規則）。記下執行檔基準 1,104,896 bytes | 既有 235 綠；新測試全 RED 且每條失敗訊息說出正確理由 | — |
| **T02 IconPalette** | `RGBA`、`IconPalette`（5 欄位，`.default` 用 spec §3 的數字，**不得為 gate 調值**）、`appearance(for:palette:reduceMotion:)`、`IconAppearance.color` | `paletteSubscriptIsTotal`、`appearanceCarriesPaletteColor` 綠；mutation：subscript `.waiting` 接到 `idle` / policy 寫死 → 紅 | T01 |
| **T03 AnimationCurve** | `LEDStripView.alpha(for:phase:)` 與 `AnimationDriver.period(of:)` 搬到 `AuraCore/AnimationCurve.swift`；兩處改呼叫它、**刪掉自己那份** | `animationCurveBoundaries`、`appLayerNeverSwitchesOnIconAnimation` 綠；mutation 0.14→0.15 紅、view 或 driver 保留 switch 紅；R4 與「動畫排程與省電」兩個 suite 綠 | T01 |
| **T04 IconDrawing 接線** | `IconDrawing` protocol（instance `preferredWidth`）、`IconForm.resolve(from:)`/`makeView()`、`StatusItemController.init(defaults:)` + `drawing` + `statusItemLength` + `removeFromStatusBar()`（**不用 deinit**）+ frame 由 view 尺寸設；`LEDStripView` conform | `iconFormSelectionIsWired`、`statusItemWidthFollowsRenderer`（含 frame 三斷言）綠；mutation：讀完不用 / 寬度或 frame 寫死 → 紅 | T02, T03 |
| **T05 A2 底板** | `LEDStripView` 依 §4.1：bounds 含底板（42×h）、不透明 `#141416`、幾何常數、顏色吃 `appearance.color` | `plateGuaranteesContrast`、`workingIsQuietestColor`、`renderersHonorPalette`(LED)、`renderersDrawEveryActivityWithoutCrash`(LED) 綠；working 峰值雙向 pin 2.54±0.10；mutation：底板 alpha→0.82 紅、working 改 #48484a 紅、working 改 #50a8ff 紅（序＋pin）、view 改回自己 switch 紅、view 忽略 color.a 紅 | T04 |
| **T06 B2 光環** | `HaloView` 依 §4.2（`preferredWidth` 16、環心 = bounds 中點、idle alpha 來自 `palette.idle.a` 無特例），幾何常數 | `haloRingAnimatesCoreDoesNot`、`renderersHonorPalette`(Halo)、`renderersDrawEveryActivityWithoutCrash`(Halo) 綠；mutation 核心也套 alpha → 紅 | T04 |
| **T07 證據與 DoD** | `./scripts/build-app.sh`；`AURA_RENDER_EVIDENCE=1` 出 E1′ 到 `docs/evidence/m4/`；兩形態各量 CPU/RSS/執行檔增量/Sources 淨增；寫 `plans/2026-09-09-m4-icon-form-dod.md`；**向使用者索取 E1 最小 4 張（＋E2 建議）**，收進 `docs/evidence/m4/` 前由使用者確認無識別資訊 | §7 DoD 除 persona 外全部有數字；E1 到位（否則 T08 只出暫定排名、T09 不執行、PR 標 blocked-on-evidence） | T05, T06 |
| **T08 persona A/B** | 3 persona 並行依 §4.4 打分 → 加權指數 → 決策規則 → `docs/2026-09-09-m4-ab-decision.md` | 報告含每分數的證據來源（E1/E1′/E3/E4）、指數、規則套用過程 | T07 |
| **T09 刪輸家＋回寫** | 刪輸家 view 與其測試（保留交叉覆蓋，spec §6）、刪 `IconForm` 與 key（`defaults delete io.agentaura.app AgentAuraIconForm`）、`iconFormSelectionIsWired` 改「永遠建贏家」；正典**逐句**回寫依 spec §8.2；CLAUDE.md／README 過時句 | repo `grep -r` 不到輸家類名與 `AgentAuraIconForm`；正典 grep「跟隨深淺色外觀」「IconRenderer」= 0；`defaults read` 無該 key；全套件綠；Sources 淨增 ≤ 250 | T08 |

**Batch**：A = T02+T03 · B = T04+T05 · C = T06 · D = T09。T01、T07 由主 session 或單獨 implementer；T08 persona-tester。
**並行**：A 內兩 task 檔案不相交，可平行但規模小、序列即可。B 之後 C 與 T07 的 build 可重疊。
**Tier 1**：動 `Sources/AgentAuraApp/**` 與 `IconAppearance.swift` → T08 必派。
