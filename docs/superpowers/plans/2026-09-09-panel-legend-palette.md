# Tasks · `change/panel-legend-palette`

Spec：`docs/superpowers/specs/2026-09-09-panel-legend-palette-design.md`（§ 編號引用它）。
每 task 一 commit、TDD、pre-flight `git branch --show-current` = `change/panel-legend-palette`。分支基於 `change/m4-icon-form`（PR #4 squash 後需 rebase）。

| Task | 內容 | 綠燈條件 | 依賴 |
|---|---|---|---|
| **T01 測試底座** | `PaletteCodecTests`、`LegendModelTests`、`PaletteStoreTests`、`PanelPixelTests`（**NSHostingView → OffscreenRender**，不用 ImageRenderer——它不渲 ScrollView 內容）、`ColorPickerTests`、新檔 `PaletteWiringSmokeTests`（`paletteChangeReachesIcon`、`pickerChainIsWired`、`persistedColorAppliesOnFirstFrame`、`legendAlwaysPresent`、`controllerForwardsPanelCallbacks`、`hostingControllerIsReused`）；`SpyRenderer` 對齊 `setPanel(_ model: PanelModel)` ＋ `setPopoverPinned` ＋兩個 callback 屬性（`CompositionSmokeTests` 只動簽章）。**compile-only stub**：`PaletteCodec`（全回 nil／空）、`IconPalette` 擴充（`with` 回 self、`isDefault` 恆 false、`customizable` 空）、`LegendItem`/`LegendModel`（回空）、`PanelModel.make`（legend 空、showsReset false）、`PaletteStore`（palette 恆 default、不讀不寫）、`ColorPickerCoordinator`（`rgba(from:)` 回 nil）、`AnimationDriver.setPalette`（空）、`IconRendering.setPanel(_:)`＋`onPickColor`/`onResetColors`/`setPopoverPinned`、`StatusItemController.popoverBehavior/panelOnPick/panelOnReset`（stub 不轉發）、`AppDelegate.init(defaults:)`＋`applyColor`/`resetColors`（空）、`LegendRowView`（空 view）、`ColorBridge`、`Activity.customizable`（空）。禁 `fatalError` | 既有 257 綠；新測試全 RED 且理由對（reviewer 實測：NSHostingView + OffscreenRender 掃色可行，10pt 圓 @2x ≈ 284 px、8pt ≈ 172–180 px；ImageRenderer 不渲 ScrollView） | — |
| **T02 AuraCore** | `PaletteCodec` 四函式（先驗長度／字元集再索引）、`Activity.customizable`、`IconPalette.with/isDefault`、`LegendModel`、`PanelModel.make`（獨立檔） | AuraCore 8 條 gate 綠；mutation：`hex` 少一位／忽略 idle 的 guard 移除／`with` 改錯欄位／`make` 忽略 palette → 各紅 | T01 |
| **T03 App 持久化與色板橋** | `PaletteStore`（只讀四 key、先 `onChange` 再 `persist()=encode`）、`ColorPickerCoordinator`（init 註冊一次 `willClose`、`hidesOnDeactivate=false`、三關 guard、`endCount`、`detach`）、`ColorBridge` | `PaletteStoreTests` 6 條（loads/roundTrip/resetRemoves/tolerates/notifiesBefore/keyBudget）、`colorPanelConversion`、`changeColorGuards`、`willCloseObservedOnce` 綠；mutation：寫錯 key／順序反／刪 guard／pick 內重複註冊／hidesOnDeactivate 沒關 → 紅 | T01 |
| **T04 接線** | `AnimationDriver.setPalette`；`IconRendering` 三成員；`StatusItemController`（hosting 重用＋`preferredContentSize`、pinned 雙路徑、轉發、`rootViewAssignments` 儀器）；`AppDelegate`：store/coordinator（internal 供 smoke）、`defaults` 注入、`setPalette` **在同步區內（第一次 icon 交付前）**、`applyColor`/`resetColors`、pin 接線 | 7 條 smoke 綠（`paletteChangeReachesIcon`、`pickerChainIsWired` 四段、`resetReachesIconAndDefaults`、`controllerForwardsPanelCallbacks`、`persistedColorAppliesOnFirstFrame`、`legendAlwaysPresent`、`hostingControllerIsReused`）；mutation：刪 `driver.setPalette`／`setPalette` 移進 Task／刪 `status.onPickColor =`／刪 `coordinator.onPick =`／永遠跳過／漏 `preferredContentSize` → 紅 | T02, T03 |
| **T05 面板 UI** | `PanelView(model:onPick:onReset:)`、`PanelRowView(row:palette:)`、`LegendRowView`（10pt 圓／20pt 命中、標籤、提示行「8 顆燈是一體…」、常駐重設 disabled） | `legendDotsUsePalette`（rows 空＋非空）、`rowDotsUsePalette` 綠；mutation：`LegendDot` 寫死色／`PanelRowView` 保留舊 switch → 紅 | T04 |
| **T06 證據、DoD、回寫** | 面板證據圖（NSHostingView 路徑）→ `docs/evidence/change2/`；量 CPU（隔離量測者 session）、執行檔、LOC（門檻 +480）、覆蓋率、key 預算；正典 §3.7／§0 D2 R7 R8 回寫、m4 帳本 F-01 列；CLAUDE.md（Tier 1 加三檔）／README「改顏色」段；DoD 帳本；`build-app.sh` 重建並重啟 app | §7 全部有數字；使用者實測項清單（釘住 popover、tap）列入報告 | T05 |
| **T07 persona** | 3 persona（新手 R7 ／ 客製化 R8 ／ 極簡）依 §7 門檻打分；證據：面板渲染圖 + 程式碼讀 + 此前 E1 | 加權 ≥6.0，硬下限過 | T06 |

**Batch**：A = T02+T03（檔案不相交）· B = T04+T05 · T06 主 session · T07 persona-tester。
**Tier 1**：`Sources/AgentAuraApp/**` → T07 必派。
